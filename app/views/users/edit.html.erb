<% provide(:title, "Edit " + @user.name) %>
<% provide(:url, user_path) %>
<% provide(:button_text, "Edit profile") %>
<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<h2>Edit <%= @user.name %></h2>
		<%= render 'users/form' %>
	</div>
</div>